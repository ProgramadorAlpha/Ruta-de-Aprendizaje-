'USE STRICT'

var pais = "Espana";
var continente = "Europa";
var antiguedad = 2019;
let  prueba = "hola mundo";

//Esto es un ejemplo de Concatenación es el proceso de anexar una cadena al final de otra cadena. Las cadenas se concatenan con el operador + aqui en JS:
var pais_y_contienente = pais+' '+continente;

//Si luego quiero cambiar el dato de una variable solo pongo el nombre de variable y el simbolo (=) asi cambio ese dato sin volver a definir la vartiable ejemplo:
// pais = Mexico; listo asi quedaria sustituido el "Pais"

console.log(pais,continente);
alert (pais_y_contienente);